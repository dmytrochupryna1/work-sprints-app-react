const ConfirmationMessage = ({ sprints }) => {
    return (
        <div className="confirmation-message">
            <h2>You have chosen to complete {sprints} sprints today!</h2>
        </div>
    )
}
export default ConfirmationMessage